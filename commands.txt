Create git repository in website.
Then use the following commands:

git clone  git clone https://github.com/kaihanahammed/<repositoryname>.git
cd <repositoryname>
ls
git status
git config --global user.name "kaihanahammed"
git config --global user.email "kaihanahammed07@gmail.com"
git config -l
git remote set-url origin https://<githubtoken>@github.com/kaihanahammed/<repositoryname>.git
git add -A
git commit -m "Message"
git push origin main

